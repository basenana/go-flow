package controller

import "github.com/zwwhdls/go-flow/plugin"

func buildHookWithDefault(hook plugin.Hook) plugin.Hook {
	return hook
}

func buildTaskHookWithDefault(hook plugin.Hook) plugin.Hook {
	return hook
}
